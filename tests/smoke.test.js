const fs=require('fs'),vm=require('vm'),assert=require('assert');const src=fs.readFileSync('index.js','utf8');new vm.Script(src);assert(src.includes("new Lampa.InteractionMain"));assert(src.includes("card_events"));assert(src.includes("Новинки"));assert(src.includes("Сейчас выходит"));assert(src.includes("Популярное"));assert(src.includes("backdrop-filter:none"));assert(!src.includes("setInterval("));assert(!src.includes("Authorization"));assert(!src.includes("grid-template-columns"));assert(src.length<15000);console.log('Anime Lite v0.2 tests passed');